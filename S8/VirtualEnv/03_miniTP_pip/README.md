# miniTP Pip

1. Qu'est-ce que `PyPI` ?
2. Vérifier la présence de `pip` sur votre session
3. Trouver la version de `pip` à l'aide de `pip --version` et voir quel `pip` est utilisé (c'est-à-dire où est stocké `pip`)
4. Lister les commandes à l'aide de `pip help`
5. Utiliser `pip list` pour trouver les packages déjà installés
6. Utiliser `pip freeze` pour trouver les packages déjà installés. Quelle est la différence ?